//task10: use for loop to iterate over array and log result to console.
let arr = [1, 2, 3, 4, 5];
for (let i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}

//task11: use ForEach method to iterate over each element in array and log it to console.
arr.forEach((ele) => console.log(ele));
