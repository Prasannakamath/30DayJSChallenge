//task6: check if a number is even or odd using ternary operator and log the result to console.
function isEven(num) {
  return num % 2 === 0 ? true : false;
}

let num = 4;
console.log(`is ${num} Even? : ${isEven(num)}`);
