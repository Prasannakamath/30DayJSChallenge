//task5: write an arrow function to find sum of 2 numbers and log the result to console.
let sum = (a, b) => a + b;
console.log(sum(3, 4));

//task6: Write an arrow function to check if a string contains specific character and return the result.
let Str = "PRASANNA";
let ch = "X";
let findChar = (str, ch) => str.includes(ch);
console.log(findChar(Str, ch));
